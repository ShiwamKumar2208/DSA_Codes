#include<bits/stdc++.h>
using namespace std;

int main () {
    // space complexity is the amount of memory used by an algorithm to run to completion
    // space complexity is measured in terms of the size of the input to the algorithm
    // space complexity is often expressed using big O notation

    // sc = auxiliary space + space used by the input
    // auxiliary space is the space used by the algorithm to run to completion, excluding the space used by the input
    // space complexity of an algorithm is the maximum amount of memory used by the algorithm to run to completion

    // a, b input;
    // c = a+b
    // then c for auxiliary space, a and b for input space

    // never do anything to input - VIMP for coding interviews
    // always try to minimize the auxiliary space used by the algorithm


    // VIMP
    // in competetive programming, almost all the servers takes
    // 1 sec to complete 10^8 operations, so we can use this as a benchmark to estimate the time complexity of our algorithm
    // in competetive programming, almost all the servers takes 1 GB of memory, so we can use this as a benchmark to estimate the space complexity of our algorithm
}